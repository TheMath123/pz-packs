{
  "name": "@org/database",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun drizzle-kit studio --port=7000 --config=./drizzle.config.ts",
    "dev:all": "bun docker:up && bun drizzle-kit studio --port=7000 --config=./drizzle.config.ts",
    "preview": "bun docker:up && bun drizzle-kit studio --port=7000 --config=./drizzle.config.ts",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate --config=./drizzle.config.ts",
    "db:migrate": "bun run ./migrate.ts",
    "type:check": "tsc --noEmit",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:kill": "docker compose down --volumes --remove-orphans"
  },
  "dependencies": {
    "@org/validation": "workspace:*",
    "drizzle-orm": "0.45.0"
  },
  "devDependencies": {
    "@org/tsconfig": "workspace:*",
    "drizzle-kit": "0.31.6",
    "pg": "8.16.3"
  }
}
